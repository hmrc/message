# Copyright 2026 HM Revenue & Customs
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

include "backend.conf"

# This is the main configuration file for the application.
# ~~~~~

appName = message

# Overriding the userAgent value so that we can avoid displaying AHC/2.1 in Kibana Logs
play.ws.useragent = ${appName}

play.modules.enabled += "uk.gov.hmrc.play.bootstrap.AuthModule"
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.HttpClientV2Module"
play.modules.enabled += "uk.gov.hmrc.message.MessageModule"

play.http.errorHandler = "uk.gov.hmrc.message.utils.ErrorHandler"

# Session Timeout
# ~~~~
# The default session timeout for the app is 15 minutes (900seconds).
# Updating this is the responsibility of the app - it must issue a new cookie with each request or the session will
# timeout 15 minutes after login (regardless of user activity).
# session.maxAge=900


# Session configuration
# ~~~~~
application.session.httpOnly = false

application.session.secure = false

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# !!!WARNING!!! DO NOT CHANGE THIS ROUTER
play.http.router = prod.Routes

# Setting to allow the body of posts to be 10MB
play.http.parser.maxMemoryBuffer = 10MB

# Controller
# ~~~~~
# By default all controllers will have authorisation, logging and
# auditing (transaction monitoring) enabled.
# The below controllers are the default exceptions to this rule.

microservice.metrics.gauges.interval = 12 hours

controllers {
  confidenceLevel = 200

  com.kenshoo.play.metrics.MetricsController = {
    needsLogging = false
    needsAuditing = false
  }
  uk.gov.hmrc {
    play.controllers.HealthCheck = {
      needsLogging = false
      needsAuditing = false
    }
    message.controllers {
      MessagesController = {
        needsAuditing = false
      }
    }
  }

  testonly.TestOnlyController = {
    needsAuditing = false
  }
}

microservice {
    metrics {
      gauges {
        interval = 15 minutes
      }
    }

    services {
      timeout = 5 seconds

      delay-response = 2 seconds

      protocol = http

      auth {
        host = localhost
        port = 8500
      }

      datastream {
        host = localhost
        port = 8100
      }
      secure-message {
         host = localhost
         port = 9051
      }
      message {
        host = localhost
        port = 8910
      }
      auth-login-api {
        host = localhost
        port = 8585
      }
    }
}

lock.forceReleaseAfter = 5 minutes

auditEventMaxSize = 128000

# Set request timeout to 35 seconds
play.ws.timeout.request = 35 seconds